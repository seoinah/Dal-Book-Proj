import {footerStyle} from "@/components/organisms/Footer/Footer.styles";
import { Layout } from 'antd'
const { Footer } = Layout;

const FooterComponent = () => {

    return (
        <Footer style={footerStyle}>Footer</Footer>
    );
};

export default FooterComponent;
# application.yml 설정
spring:
  profiles:
    active: local
  devtools:
    livereload:
      enabled: true

# server 관련 설정
server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true